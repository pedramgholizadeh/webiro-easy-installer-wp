<?php defined( 'ABSPATH' ) OR die( 'This script cannot be accessed directly.' );

// Change default appearance of input fields due to historical Zephyr defference
$config['params']['items']['params']['move_label']['std'] = '1';

return $config;
